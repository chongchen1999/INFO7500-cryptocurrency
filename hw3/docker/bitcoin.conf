# bitcoin.conf
# 基础配置
server=1
rest=1
txindex=0  # 关闭全交易索引（节省空间）
prune=550  # 启用修剪模式，保留550MB区块数据

# RPC配置
rpcuser=myrpcuser
rpcpassword=myrpcpassword
rpcallowip=127.0.0.1  # 只允许本地访问
rpcport=8332

# 网络配置
listen=1
port=8333

# 内存配置
maxmempool=200  # 内存池最大200MB
dbcache=100      # 数据库缓存100MB